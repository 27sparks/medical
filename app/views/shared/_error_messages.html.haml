- if item.errors.any?
  #error_explanation
    .alert.alert-error
      %h4= "The form contains #{ pluralize(item.errors.count, "error")}"
      %ul
        - item.errors.full_messages.each do |msg| 
          %li= "#{msg}"
 